# DINOv3 embedding extraction (optional; install on top of requirements.txt)
# CPU:
#   pip install -r requirements-dinov3.txt
# CUDA 12.1 Linux example:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#   pip install -r requirements-dinov3.txt --no-deps transformers accelerate
pillow
numpy
pandas
torch
torchvision
transformers>=4.57
accelerate
safetensors